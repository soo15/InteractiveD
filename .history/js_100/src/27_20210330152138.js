const key = ["Yujin" "Hyewon"].split(' ');
const value = [70 100].split(' ');

const keys = key.split(' ');
console.log(values)
const obj = {};

for(let i =0; i < keys.lenght; i++){
    obj[key[i]] = parseInt(value[i], 10);
}

console.log(obj);